# Origin of the deployed Express backend (Railway). Leave unset for local dev —
# Vite's dev server proxies /api to http://localhost:4000 automatically.
VITE_API_URL=https://your-railway-app.up.railway.app
